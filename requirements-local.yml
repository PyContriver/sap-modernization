---
# Public Galaxy only (no Automation Hub token) — pair with terraform_driver: local
collections:
  - name: cloud.terraform
    version: ">=2.0.0"
  - name: amazon.aws
    version: ">=7.0.0"
  - name: ansible.posix
    version: ">=1.5.0"

roles: []
