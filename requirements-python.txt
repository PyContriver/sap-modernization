# Python libs for localhost Ansible runs (macOS/Linux). Install via: ./scripts/setup-local-venv.sh
# amazon.aws preflight / golden image
boto3>=1.34
botocore>=1.34
# hashicorp.terraform (HCP Terraform API)
pytfe>=0.0.1
