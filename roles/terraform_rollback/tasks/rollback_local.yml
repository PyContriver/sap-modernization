---
- name: Destroy infrastructure with cloud.terraform CLI
  cloud.terraform.terraform:
    project_path: "{{ terraform_project_path }}"
    state: absent
    force_init: true
    backend_config: "{{ terraform_backend_config }}"
    lock: true
